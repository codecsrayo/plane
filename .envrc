use flake

# Automatically load the Nix development environment
# This file is used by direnv to load the environment defined in flake.nix
# Install direnv and run 'direnv allow' to enable automatic environment loading
